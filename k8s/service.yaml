apiVersion: v1
kind: Service
metadata:
  name: zero-cloud-service
spec:
  selector:
    app: zero-cloud
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
  type: NodePort